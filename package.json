{
  "name": "fanpesa-bot-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Stateless Cloudflare Worker for the FanPesa Telegram bot",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "node --check src/worker.js"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
